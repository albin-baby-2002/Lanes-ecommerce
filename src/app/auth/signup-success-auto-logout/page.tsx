import React from "react";

// page to show success after signup but loggedout because failed to save kinde data of user to db

const LogoutAfterSignup = () => {
  return (
    <div>
      <p>Failed to add data</p>
    </div>
  );
};

export default LogoutAfterSignup;
